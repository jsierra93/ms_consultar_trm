spring:
  application:
    name: consultartrm
  data:
    mongodb:
      #authentication-database: admin
      #username: root
      #password: root
      database: TrmColombia
      port: 27017
      host: localhost
server:
  port: 8081
